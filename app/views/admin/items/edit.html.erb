<div class="row">
  <br>
  <div class="large-8 columns">
    <h1>Editing <%= "#{@item.name}" %></h1>
  </div>
  <div class="large-4 columns">
      <%# link_to 'View Item', admin_item_path(@item), class: "button" %>
    <%= link_to 'Back to all Items', admin_items_path, class: "button" %>
  </div>
</div>

<br>

<%= render 'form' %>
