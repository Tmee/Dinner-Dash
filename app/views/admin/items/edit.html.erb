<div class="row">
  <br>
  <div class="large-6 columns"><h1>Editing <%= "#{@item.name}" %></h1></div>
  <div class="large-6 columns"><%= link_to 'View Item', admin_item_path(@item) %> | <%= link_to 'Back to all Items', admin_items_path %></div>
</div>

<%= render 'form' %>
