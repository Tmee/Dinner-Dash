<div class="row">
  <div class="large-9 columns">
    <h1>Your Cart</h1>
  </div>
  <% if !current_user.present? %>
  <div class="large-3 columns">
    Please Log In to complete your order
  </div>
  <% else %>
  <div class="large-3 columns">
    <%= form_tag '/orders' do |f| %>
      <%= submit_tag "Place my Order", class: "button" %>
    <% end %>
  </div>
  <%end%>
</div>

  <div class="row">
    <div class="large-9 columns">
    <table>  
      <thead>
        <tr>
          <th class="text-left"></th>
          <th class="text-left">Item</th>
          <th class="text-left">Fillings</th>
          <th class="text-left">Quantity</th>
          <th class="text-left">Price</th>
          <th class="text-left"></th>
        </tr>
      </thead>
      <tbody>
        <% @cart_items.each do |line_item| %>
          <tr>
            <td class="text-left"><%= image_tag line_item[:item].image.url :thumb %></td>
            <td class="text-left"><h3><%= line_item[:item].name %></h3></td>
            <td class="text-left"><% line_item[:fillings].each do |filling| %>
                  <%= filling.title %>: $<%= filling.price / 100 %> (extra),
                <% end %> </td>
            <td class="text-left"><%= form_tag "/cart" do %>
               <input type="number" name="quantity" id="quantity" min="1" max="30" value="<%= line_item[:quantity] %>"/>
               <input type="hidden" name="id", value="<%= line_item[:id] %>" />
               <button type= "submit" class="tiny button" >Update</button>
             <% end %>
            </td> 
            <td class="text-left">$ <%= line_item[:price] / 100 %></td>
            <td class="text-left"><%= link_to "Remove Item", cart_path(:id => line_item[:id]), method: :delete %>
            </td> 
          </tr>
        <% end %>
         <tr>
            <td class="text-left"></td>
            <td class="text-left"></td>
            <td class="text-left"></td>
            <td class="text-left">TOTAL:</td>
            <td class="text-left">$ <%= @total_price /100 %></td>
          </tr>
      </tbody>
    </table>
  </div>




